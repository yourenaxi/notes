cnpm i webpack -D        局部安装wenpack
cnpm i webpack-cli -D        局部安装wenpack-cli


cnpm i webpack-dev-server -D  自动打包工具   需要在package.js的script里面配置 "dev":"webpack-dev-server"

"dev":"webpack-dev-server" 有以下参数
--open 自动打开浏览器
--port 3000  指定端口号
--contentBase 内容路径 src
--hot  热更新

cnpm i style-loader css-loader -D  使用webpack打包css
cnpm i url-loader file-loader --save-dev  处理 css 路径

module :{       //用于打包第三方loader 模块的
 
   reles:[   //文件的匹配规则
            { test:/\.css$/,use:['style-loader','css-loader'] },
            { test: /\.(png|jpg|gif)$/, use: 'url-loader' }
       ]
    }

---------------------------------------------------------------
cnpm i vue-loader vue-template-compiler -D  解析 vue的loader

const VueLoaderPlugin = require('vue-loader/lib/plugin');
 plugins: [
        new VueLoaderPlugin()
    ]
在 vue 组件中使用路由  

import VueRouter from 'vue-router'      导入路由模块

Vue.use(VueRouter)                     使用路由模块

----------------------------------------------------------
cnpm i mint-ui -S           //安装了饿了么组件库


import MintUI from "mint-ui"   //饿了么组件库

import "mint-ui/lib/style.css"  //导入样式

Vue.use(MintUI)        //在Vue中使用MintUI



 {test:/\.(woff|ttf|eot|otf)$/,use:"file-loader"}










